import 'package:flutter/material.dart';

class MainPenjadwalan extends StatelessWidget{
  const MainPenjadwalan({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(
        child: ListView(
          children: [
            Text("tot")
          ].toList()
        )
      ),
    );
  }
}